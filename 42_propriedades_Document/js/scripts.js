const log = console.log;

log(document.body)

log(document.head);

log(document.links);

log(document.links[0]);

document.links[0].textContent = 'Twitter';

log (document.URL)

log(document.title);

document.title = 'Aula 42';

log(document.title);